# Gemini CLI 技能参考指南 (Agent Skills Reference)

本文件记录了当前 AI 助手已加载的各项技能（Skills）及其核心功能，方便你随时查看如何更好地利用这些工具来管理你的 Obsidian 笔记库。

---

## 🛠️ 已激活核心技能

### 1. **Obsidian Markdown (`obsidian-markdown`)**
*   **定位**：基础语法增强。
*   **功能**：
    *   处理 `[[双链]]` 和 `![[嵌入]]`。
    *   创建和编辑 Obsidian `Callouts`（如 `> [!INFO]`）。
    *   管理文档属性（Frontmatter/YAML Properties）。
    *   确保生成的所有内容完全符合 Obsidian 的原生格式要求。

### 2. **Obsidian CLI (`obsidian-cli`)**
*   **定位**：自动化库操作。
*   **功能**：
    *   **全局搜索**：在整个库中查找特定内容或任务。
    *   **重构支持**：移动或重命名文件时，自动更新全库的所有引用链接，防止出现“死链”。
    *   **插件管理**：可以辅助开发、调试及运行 Obsidian 插件。

### 3. **Obsidian Bases (`obsidian-bases`)**
*   **定位**：数据库化管理。
*   **功能**：
    *   处理 `.base` 文件，实现类似 Notion Database 的功能。
    *   支持创建表格视图、卡片视图，并应用复杂的过滤器（Filters）和公式（Formulas）。

### 4. **JSON Canvas (`json-canvas`)**
*   **定位**：可视化思维建模。
*   **功能**：
    *   通过代码生成或修改 `.canvas` 画布文件。
    *   自动连接笔记节点、创建逻辑分组，将抽象的知识结构化、视觉化。

### 5. **Defuddle (`defuddle`)**
*   **定位**：网页知识采集。
*   **功能**：
    *   从给定的 URL 中提取“脱水”后的 Markdown 内容。
    *   去除网页广告、导航等杂质，非常适合将外部技术文档快速转入 Obsidian。

---

## 🛡️ 受限/特殊技能

### 6. **国网笔记小助手 (`state-grid-assistant`)**
*   **状态**：按需激活。
*   **触发方式**：必须在对话中明确要求 **“调用国网笔记小助手”**。
*   **功能**：针对国家电网相关的电力系统专业知识（如短路分析、电气设备原理等）提供深度优化的笔记整理、知识问答和架构建议。

### 7. **技能创造器 (`skill-creator`)**
*   **定位**：扩展性工具。
*   **功能**：指导如何创建新的 Skill 或更新现有 Skill。如果你发现有重复性的任务目前没有对应技能，可以要求我使用此工具来“进化”。

---

## 🧠 使用建议
你可以结合多个技能来执行复杂指令，例如：
> “使用 **defuddle** 抓取这篇电力系统论文，然后利用 **obsidian-markdown** 将其转化为带 Callout 的笔记，最后用 **obsidian-cli** 将它链接到我现有的‘短路分析’目录中。”

---
*最后更新日期：2026年4月30日*