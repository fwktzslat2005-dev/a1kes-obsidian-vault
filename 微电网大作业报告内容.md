# 智能微电网大作业——交流微电网设计

---

## 一、封面格式

```
┌─────────────────────────────────────────────────────┐
│                                                       │
│                                                       │
│              期末论文作业报告                          │
│                                                       │
│                                                       │
│    课程名称：  智能微电网                              │
│                                                       │
│    班级名称：  _______________                        │
│                                                       │
│    学生姓名：  _______________                        │
│                                                       │
│    学生学号：  _______________                        │
│                                                       │
│    任课教师：  _______________                        │
│                                                       │
│    报告成绩：  _______________                        │
│                                                       │
│    评阅教师签字：_______________                      │
│                                                       │
│                  年    月    日                       │
│                                                       │
│              电力工程学院                             │
└─────────────────────────────────────────────────────┘
```
> 封面格式说明：各下划线右端对齐，可用Tab或表格方式排版，居中放置，字体为宋体小二号加粗（标题），其余为宋体四号。

---

## 二、微电网提出的背景知识（约1000字）

### 2.1 能源转型与分布式发电的兴起

随着全球化石能源的日益枯竭和环境污染问题的加剧，各国纷纷将发展清洁能源和可再生能源作为能源战略的重要方向。太阳能、风能等分布式可再生能源发电技术得到了广泛关注和快速发展。分布式发电（Distributed Generation, DG）是指布置在用户附近的、规模较小的发电单元，其容量通常在几十千瓦到几兆瓦之间。分布式发电具有靠近负荷中心、输配电线损小、安装灵活、环境友好等优点。

### 2.2 微电网概念的提出

然而，大规模分布式电源直接接入配电网也带来了一系列问题：光伏和风电的出力具有间歇性和波动性，会对配电网的电能质量、电压稳定性和保护配合产生不利影响；大量分布式电源的接入使得传统配电网从无源网络变为有源网络，潮流分布和控制变得更加复杂；当主电网发生故障时，为保障电网安全，分布式电源必须与主网解列，无法继续为本地负荷供电。

为解决上述矛盾，美国电力可靠性技术解决方案协会（CERTS）于2002年提出了微电网（Microgrid）的概念。微电网是由分布式电源、储能装置、负荷、保护与控制装置等组成的小型发配电系统，能够实现自我控制、保护和能量管理。微电网既可以并网运行，也可以在主电网故障时脱离主网独立运行（离网/孤岛运行），从而充分发挥分布式电源的效益，同时提高供电可靠性。

### 2.3 微电网的关键技术

微电网涉及多项关键技术：（1）**分布式电源技术**——包括光伏发电、风力发电、微型燃气轮机、燃料电池等；（2）**储能技术**——蓄电池储能、飞轮储能、超级电容器等，用于平抑功率波动和维持离网时的电压频率稳定；（3）**电力电子变换技术**——DC/DC变换器、DC/AC逆变器等是实现分布式电源和储能接入的关键设备；（4）**能量管理技术**——通过能量管理系统（EMS）对微电网内各单元进行协调控制和优化调度；（5）**微电网保护与控制技术**——包括并/离网无缝切换、功率平衡控制、电压频率调节等。

### 2.4 发展现状与趋势

目前，微电网技术已在全球范围内得到示范应用和推广。美国、欧盟、日本和中国都建立了多个微电网示范工程。我国在"十四五"规划中明确提出要建设新型电力系统，微电网作为配电网层面的重要组成部分，在提高新能源消纳能力、提升供电可靠性、推动能源绿色低碳转型方面具有广阔的应用前景。

（共计约980字）

---

## 三、一次电气图

### 3.1 一次接线图说明

交流微电网采用单母线结构，380V交流母线为系统核心，各单元经相应变换装置和开关设备并入母线。系统通过一台400V/10kV升压变压器经PCC（公共连接点）开关与配电网连接。

### 3.2 一次电气图（手绘图参考）

```
                          380V 交流母线
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
   ┌────┴────┐           ┌────┴────┐           ┌────┴────┐
   │  PCC    │           │   QF1   │           │   QF3   │
   │  开关   │           │         │           │         │
   └────┬────┘           └────┬────┘           └────┬────┘
        │                     │                     │
   ┌────┴────┐           ┌────┴────┐           ┌────┴────┐
   │ 升压变  │           │  DC/AC  │           │  AC/DC  │
   │400V/10kV│           │  逆变器  │           │  整流器  │
   └────┬────┘           │  100kW  │           │  50kW   │
        │                └────┬────┘           └────┬────┘
   ┌────┴────┐                │                     │
   │ 10kV    │           ┌────┴────┐           ┌────┴────┘
   │ 配电网  │           │  DC/DC  │                │
   └─────────┘           │  Boost  │                ├──────────┐
                         └────┬────┘                │          │
                              │                ┌────┴────┐     │
                         ┌────┴────┐           │  AC/DC  │     │
                         │ 光伏阵列 │           │  变流器 │     │
                         │ 100kWp  │           │  (机侧) │     │
                         └─────────┘           └────┬────┘     │
                                                   │          │
                                              ┌────┴────┐     │
                                              │ 永磁同步 │     │
                                              │ 发电机   │     │
                                              └────┬────┘     │
                                                   │          │
                                              ┌────┴────┐     │
                                              │ 风力机   │     │
                                              │ 50kW    │     │
                                              └─────────┘     │
                                                         ┌────┴────┐
 ┌──────────┐    ┌──────────┐                           │  AC/DC  │
 │ 重要负荷 │    │ 非重要   │                           │ 变流器  │
 │  30kW    │    │ 负荷     │                           │ (网侧)  │
 │  (经     │    │  100kW   │                           └────┬────┘
 │  QF4接入)│    │  (经     │                                │
 └──────────┘    │  QF5接入)│                          ┌────┴────┐
                 └──────────┘                          │  DC/DC  │
                                                       │ 变换器  │
                                                       └────┬────┘
                                                       ┌────┴────┐
                                                       │ 储能电池 │
                                                       │200kW×2h │
                                                       │(400kWh) │
                                                       └─────────┘
```

### 3.3 各设备并网电气结构

| 设备 | 并网方式 | 关键设备 |
|------|---------|---------|
| 光伏100kW | DC/DC Boost → DC/AC逆变器 → 交流母线 | MPPT控制器、三相全桥逆变器、LCL滤波器 |
| 风电50kW | 永磁同步发电机 → 机侧AC/DC → 直流母线 → 网侧DC/AC → 交流母线 | 背靠背双PWM变流器 |
| 储能200kW×2h | 锂电池组 → DC/DC双向变换器 → DC/AC双向变流器 → 交流母线 | 双向DC/DC、双向DC/AC、BMS |
| 重要负荷30kW | 直接经断路器接入交流母线 | 断路器QF4 |
| 非重要负荷100kW | 经断路器(可控)接入交流母线 | 断路器QF5、接触器 |
| 配电网 | 经PCC开关→升压变压器→10kV配电网 | PCC断路器、400V/10kV变压器 |

### 3.4 微电网接地方式

系统采用**TN-S接地方式**（三相五线制）。变压器中性点直接接地，PE线和N线分开敷设。所有设备外壳、逆变器机壳、光伏支架、风机塔筒均通过PE线与接地网可靠连接。接地电阻不大于4Ω。在PCC处设置剩余电流保护装置。

---

## 四、能量管理系统（EMS）结构图

### 4.1 EMS总体架构

```
                     ┌─────────────────────────────┐
                     │      云端监控平台(可选)       │
                     │   Web/APP 远程监控           │
                     └─────────────┬───────────────┘
                                   │ Internet/4G
                     ┌─────────────┴───────────────┐
                     │    EMS 中央控制服务器        │
                     │  ┌───────────────────────┐  │
                     │  │ • 数据采集与存储       │  │
                     │  │ • 功率预测(光伏/风电)  │  │
                     │  │ • 优化调度算法         │  │
                     │  │ • 模式切换逻辑         │  │
                     │  │ • 人机界面(HMI)        │  │
                     │  │ • 数据库服务器         │  │
                     │  └───────────────────────┘  │
                     └─────────────┬───────────────┘
                                   │ 工业以太网 (Modbus TCP / IEC 61850)
          ┌──────────────┬─────────┼─────────┬──────────────┐
          │              │         │         │              │
   ┌──────┴──────┐ ┌────┴────┐ ┌──┴──┐ ┌───┴────┐  ┌──────┴──────┐
   │ 光伏就地    │ │ 风电就地│ │储能 │ │负荷控制│  │  PCC/并网   │
   │ 控制器      │ │ 控制器  │ │就地 │ │单元     │  │  控制单元   │
   │             │ │         │ │控制器│ │         │  │             │
   │ • MPPT控制  │ │• 变流器 │ │      │ │• 负荷   │  │ • 并网开关  │
   │ • 逆变控制  │ │  控制   │ │• BMS │ │  投切   │  │ • 电能质量  │
   │ • 保护功能  │ │• 桨距角 │ │• SOC │ │• 负荷   │  │ • 保护控制  │
   │ • 数据采集  │ │  控制   │ │  监测│ │  功率   │  │ • 电表数据  │
   └──────┬──────┘ └────┬────┘ └──┬───┘ │  监测    │  └──────┬──────┘
          │              │         │     └───┬────┘         │
          └──────────────┴─────────┴─────────┴──────────────┘
                                   │ RS485/CAN/Modbus RTU
                          ┌────────┴────────┐
                          │  底层设备层     │
                          │ • 智能电表      │
                          │ • 逆变器        │
                          │ • 变流器        │
                          │ • BMS           │
                          │ • 环境监测仪    │
                          │   (辐照/风速)   │
                          └─────────────────┘
```

### 4.2 通信结构

| 层级 | 通信方式 | 协议 | 说明 |
|------|---------|------|------|
| 云端层 ↔ EMS服务器 | 4G/以太网 | MQTT/HTTP | 远程监控、数据上传 |
| EMS服务器 ↔ 就地控制器 | 工业以太网 | Modbus TCP / IEC 61850 | 实时数据交换、控制指令 |
| 就地控制器 ↔ 底层设备 | 现场总线 | RS485 / CAN / Modbus RTU | 逆变器、电表、BMS等设备数据采集 |
| EMS服务器 ↔ HMI | 以太网 | TCP/IP | 本地监控界面 |

### 4.3 EMS硬件配置

| 设备名称 | 规格/型号参考 | 数量 | 用途 |
|----------|-------------|------|------|
| EMS中央控制器 | 工业PC / IPC-610L + 研华DAQ板卡 | 1台 | 核心运算与调度 |
| 数据库服务器 | Dell PowerEdge T350 / SQL Server | 1台 | 历史数据存储 |
| 工业以太网交换机 | MOXA EDS-408A 8口管理型 | 2台 | 网络通信冗余 |
| HMI操作员站 | 研华工业平板显示器 21.5寸 | 1台 | 本地监控操作 |
| 光伏就地控制器 | PLC S7-1200 + 模拟量模块 | 1套 | 光伏数据采集与控制 |
| 风电就地控制器 | PLC S7-1200 + 模拟量模块 | 1套 | 风电数据采集与控制 |
| 储能就地控制器 | PLC S7-1200 + BMS通信网关 | 1套 | 储能充放电管理 |
| 负荷控制单元 | PLC S7-1200 + 数字量输出模块 | 1套 | 负荷投切控制 |
| 智能电表 | 威胜DTSD341-MC3 多功能电能表 | 6台 | 各支路电量计量 |
| 环境监测仪 | 辐照度传感器 + 风速风向仪 | 2套 | 光资源/风资源监测 |
| UPS电源 | 3kVA在线式UPS | 2台 | EMS控制电源保障 |
| GPS/北斗时钟 | 卫星同步时钟装置 | 1台 | 系统对时 |

---

## 五、微电网运行操作步骤

### 5.1 离网启动步骤

1. **启动前检查**：确认PCC开关在分闸位置，微电网与配电网已解列；确认各断路器QF1~QF5在分闸位置；检查储能系统SOC≥60%；检查各设备状态正常、无故障报警。

2. **储能系统启动**：闭合储能支路断路器QF3；启动储能变流器PCS，设置为V/f（恒压恒频）控制模式；设定输出电压380V/50Hz；PCS建立交流母线电压和频率，作为离网运行的主电源（组网单元）。

3. **光伏系统投入**：闭合光伏支路断路器QF1；光伏逆变器检测到交流母线电压正常后，经延时同步并启动；逆变器工作在PQ控制模式（恒功率模式），执行MPPT跟踪输出。

4. **风电系统投入**：闭合风电支路断路器QF2；风电变流器检测到交流母线电压正常后，经延时同步并启动；风机并网，工作在PQ控制模式。

5. **负荷投入**：按负荷优先级依次投入。先闭合QF4投入重要负荷30kW；根据功率裕量决定是否投入非重要负荷。若光伏和风电出力大于负荷需求，多余功率由储能吸收充电；若出力不足，由储能放电补充。

6. **离网稳定运行**：EMS实时监测各单元功率、储能SOC、母线电压频率，储能PCS维持电压频率稳定。

### 5.2 并网启动步骤

1. **启动前检查**：确认配电网侧电压正常（380V±7%）；确认微电网内各断路器在分闸位置；确认各设备状态正常。

2. **储能系统启动**：闭合QF3，储能PCS以PQ控制模式启动（并网模式下不自建电压频率），初始功率设为0（待机）。

3. **光伏与风电投入**：依次闭合QF1、QF2；光伏逆变器和风电变流器检测电网电压正常后并网发电，均工作在PQ控制模式。

4. **PCC并网合闸**：EMS检测微电网侧电压频率与配电网侧同步（同期条件：电压差<5%、频率差<0.2Hz、相角差<5°）；满足同期条件后，闭合PCC开关，微电网并网运行。

5. **负荷投入**：投入重要负荷QF4和非重要负荷QF5。

6. **并网稳定运行**：EMS根据电网调度指令或削峰填谷策略调节各电源出力，储能参与功率平滑或削峰填谷。

### 5.3 并网转离网无缝切换步骤

**目标**：切换过程中重要负荷（30kW）不间断供电，电压波动≤±10%，频率波动≤±0.5Hz，切换时间≤20ms。

1. **切换触发**：EMS检测到配电网侧电压跌落（<85%额定值）或频率越限或接收到孤岛保护信号。

2. **非重要负荷切除**：EMS立即发出指令，在10ms内断开QF5，切除100kW非重要负荷，减小功率缺口。

3. **储能PCS模式切换**：储能PCS从PQ控制模式切换为V/f控制模式。PCS记录切换前瞬间电压幅值和相位，以该值为初始值建立交流母线电压，保证切换前后电压连续。

4. **PCC开关分闸**：断开PCC开关，微电网与配电网解列。

5. **功率快速平衡**：EMS计算离网后净功率差额 P_net = P_load - P_pv - P_wind。若P_net>0（缺额），储能放电补充；若P_net<0（盈余），储能充电吸收，必要时限制光伏/风电出力。

6. **频率电压恢复**：储能PCS通过下垂控制和二次调频将频率精确恢复至50Hz，电压恢复至380V。

### 5.4 离网转并网无缝切换步骤

1. **同期条件检测**：EMS监测配电网侧电压状态恢复正常（持续稳定≥1分钟）；读取PCC配电网侧电压幅值、频率、相角。

2. **微电网侧电压频率调整**：EMS向储能PCS下发电压频率调节指令；通过储能PCS的二次调频调压，使微电网侧电压幅值、频率逐步接近配电网侧，直至满足同期条件（电压差<5%、频率差<0.2Hz、相角差<5°）。

3. **同期合闸**：同期条件满足后，EMS发出合闸指令闭合PCC开关；微电网并入配电网。

4. **储能PCS模式切换**：PCC合闸后，储能PCS从V/f控制模式切换为PQ控制模式；切换在1~2个工频周期内完成。

5. **非重要负荷恢复**：闭合QF5，恢复100kW非重要负荷供电。

6. **并网运行恢复**：光伏和风电继续在PQ模式下运行；EMS执行并网优化调度策略。

### 5.5 离网停运步骤

1. **负荷退出**：先断开QF5切除非重要负荷；再断开QF4切除重要负荷（或经用户确认后切除）。

2. **分布式电源退出**：降低光伏逆变器和风电变流器输出功率至零；断开QF1（光伏）和QF2（风电）。

3. **储能系统退出**：储能PCS将输出功率降至零；断开QF3；PCS停机。

4. **系统完全断电**：确认所有开关均在分闸位；记录停运时间和运行数据。

### 5.6 并网停运步骤

1. **功率降出力**：EMS向光伏、风电、储能下发降功率指令，各电源逐步降低输出功率至零。

2. **负荷退出**：依次断开QF5、QF4，切除非重要负荷和重要负荷。

3. **电源退出**：断开QF1、QF2、QF3，各电源支路解列。

4. **PCC分闸**：各电源支路断开后，断开PCC开关，微电网与配电网解列。

5. **记录归档**：记录停运时间和当日运行数据，生成运行报表。

### 5.7 离网功率平衡控制步骤

**目标**：离网运行时维持微电网内有功功率实时平衡，保证频率稳定在50±0.2Hz。

1. **主电源角色分配**：离网模式下储能PCS作为主电源（组网单元），运行在V/f控制模式，承担系统不平衡功率；光伏和风电为从电源，运行在PQ模式。

2. **实时功率计算**：
   - EMS实时采集：P_pv（光伏出力）、P_wind（风电出力）、P_load（总负荷）、SOC（储能荷电状态）
   - 计算净功率差额：ΔP = P_load - P_pv - P_wind
   - 若ΔP>0，储能放电功率 P_bat = ΔP（受限于PCS额定功率和SOC下限）
   - 若ΔP<0，储能充电功率 P_bat = -ΔP（受限于PCS额定功率和SOC上限）

3. **频率-有功下垂控制**：储能PCS采用P-f下垂特性，频率偏差与有功功率偏差成比例关系：Δf = -k_p * ΔP（k_p为下垂系数）。当系统有功不足时频率下降，PCS自动增加有功输出；当有功过剩时频率上升，PCS自动减少输出。

4. **SOC保护管理**：
   - 当SOC降至20%以下：EMS发出告警；若非必要，切除非重要负荷，降低系统负荷；
   - 当SOC降至10%以下：切除所有非必要负荷，仅保留重要负荷；必要时启动紧急停运流程。

5. **备用功率管理**：EMS始终保持储能PCS有足够的备用充放电容量应对突发功率波动；当光伏/风电出力突变时（如云层遮挡、风速变化），储能快速响应（响应时间<20ms）平抑波动。

6. **二次调频**：在下垂控制基础上，EMS根据频率偏差进行积分调节，逐步消除稳态频率偏差，将频率精确恢复至50Hz。

### 5.8 并网时参与配电网削峰填谷步骤

1. **分时电价获取**：EMS通过通信接口从电网/售电公司获取次日分时电价曲线或直接获取削峰填谷调度指令。典型分时电价时段：峰时段 8:00-12:00、17:00-21:00；平时段 12:00-17:00、21:00-24:00；谷时段 0:00-8:00。

2. **日前优化调度**：EMS结合次日光伏和风电的功率预测、负荷预测、分时电价和储能SOC约束，以日运行成本最小化为目标，建立优化模型并求解，生成储能次日充放电计划。

3. **储能充电（填谷）**：在谷电价时段（0:00-8:00），若SOC<90%，储能以设定功率从电网充电；同时光伏不发电（夜间），风电若有出力且负荷较低时，多余风电也可为储能充电。

4. **储能待机（平时段）**：在平时段（12:00-17:00），储能保持待机或小功率平滑光伏/风电波动；SOC维持在中高水平，为晚高峰放电做准备。

5. **储能放电（削峰）**：在峰电价时段（8:00-12:00、17:00-21:00），储能以设定功率放电，减少微电网从配电网的购电功率；优先使用光伏和风电出力供给负荷，不足部分由储能放电补充。

6. **实时修正**：EMS实时监测实际负荷和光伏/风电出力与预测值的偏差；若偏差超过阈值（如±10%），以5分钟为周期滚动修正储能的充放电功率，确保PCC交换功率满足削峰填谷目标。

7. **约束条件**：在整个削峰填谷过程中，EMS确保：
   - 储能SOC始终在20%~90%的安全范围内
   - 充放电功率不超过PCS额定功率200kW
   - 电池充放电倍率不超过1C
   - PCC交换功率不超出变压器容量限制

---

## 六、附录A——教材错误勘误（加分项示例）

（请根据实际使用的教材查找并填写，此处给出格式示例）

**教材**：《微电网技术与应用》XXX主编  XXX出版社  第X版

**勘误1**：
- 位置：第X章第Y节（第Z页）
- 原文："..."
- 错误描述：...
- 应更正为："..."
- 更正理由：...

---

## 七、附录B——离网功率平衡控制程序（加分项）

以下使用Python语言实现离网功率平衡控制的基本逻辑。程序模拟了离网运行时储能PCS根据净功率差额调节充放电功率、维持系统功率平衡的过程。包含P-f下垂控制和SOC保护功能。

```python
"""
微电网离网功率平衡控制程序
功能：实现离网模式下储能PCS的功率平衡控制
      包括P-f下垂控制、SOC保护、非重要负荷管理
"""

import time
import numpy as np
from dataclasses import dataclass
from typing import Optional


@dataclass
class SystemState:
    """微电网系统状态"""
    p_pv: float = 0.0        # 光伏出力 (kW)
    p_wind: float = 0.0      # 风电出力 (kW)
    p_load_imp: float = 0.0  # 重要负荷 (kW)
    p_load_nonimp: float = 0.0  # 非重要负荷 (kW)
    soc: float = 60.0        # 储能SOC (%)
    freq: float = 50.0       # 系统频率 (Hz)
    voltage: float = 380.0   # 交流母线电压 (V)
    p_battery: float = 0.0   # 储能功率 (kW，正=放电，负=充电)


class OffGridPowerBalanceController:
    """离网功率平衡控制器"""

    # 额定参数
    FREQ_RATED = 50.0           # 额定频率 (Hz)
    VOLTAGE_RATED = 380.0       # 额定电压 (V)
    P_BAT_MAX = 200.0           # 储能PCS额定功率 (kW)
    P_BAT_CHARGE_MAX = -200.0   # 最大充电功率
    P_BAT_DISCHARGE_MAX = 200.0 # 最大放电功率

    # SOC保护阈值
    SOC_HIGH = 90.0    # SOC上限 (%)
    SOC_LOW = 20.0     # SOC告警下限 (%)
    SOC_CRITICAL = 10.0  # SOC紧急下限 (%)

    # 下垂系数
    K_PF = 0.02          # P-f下垂系数 (Hz/kW)
    # 含义：100kW功率缺额对应频率下降2Hz

    # 频率允许范围
    FREQ_MIN = 49.5      # 频率下限 (Hz)
    FREQ_MAX = 50.5      # 频率上限 (Hz)

    # 控制周期
    CONTROL_PERIOD = 0.02  # 20ms控制周期

    def __init__(self):
        self.state = SystemState()
        self.nonimp_load_on = True  # 非重要负荷投入状态
        self.freq_integral = 0.0    # 二次调频积分项
        self.KI = 0.5               # 二次调频积分系数

    def read_sensors(self) -> SystemState:
        """
        读取传感器数据（实际应用中通过Modbus/RTU从各就地控制器获取）
        此处为模拟数据
        """
        # 实际实现中应从通信接口读取：
        # p_pv = modbus_read(addr=1, register=0x100)
        # p_wind = modbus_read(addr=2, register=0x100)
        # soc = modbus_read(addr=3, register=0x200)
        # etc.
        return self.state

    def calculate_power_balance(self) -> float:
        """
        计算净功率差额
        返回：ΔP (kW)，正值为功率缺额，负值为功率盈余
        """
        p_load_total = self.state.p_load_imp
        if self.nonimp_load_on:
            p_load_total += self.state.p_load_nonimp

        p_gen_total = self.state.p_pv + self.state.p_wind
        delta_p = p_load_total - p_gen_total
        return delta_p

    def pf_droop_control(self, delta_p: float) -> float:
        """
        P-f下垂控制
        输入：功率差额 delta_p (kW)
        输出：储能功率指令 p_bat_ref (kW)
        原理：频率偏差 Δf = -k_pf × P_unbalance
              储能提供不平衡功率来平衡系统
        """
        # 计算当前频率偏差
        freq_error = self.state.freq - self.FREQ_RATED

        # 下垂特性：频率偏差转换为功率补偿量
        # 当频率低于50Hz时，需要增加放电（或减少充电）
        p_droop = -(freq_error / self.K_PF)

        # 总储能功率指令 = 净功率差额 + 下垂补偿
        p_bat_ref = delta_p + p_droop

        return p_bat_ref

    def secondary_freq_regulation(self) -> float:
        """
        二次调频：消除一次调频后的稳态频率偏差
        """
        freq_error = self.state.freq - self.FREQ_RATED
        self.freq_integral += freq_error * self.CONTROL_PERIOD

        # 积分补偿量
        p_secondary = -self.KI * self.freq_integral

        # 积分抗饱和
        p_secondary = np.clip(p_secondary, -50.0, 50.0)

        return p_secondary

    def soc_protection(self, p_bat_ref: float) -> float:
        """
        SOC保护：根据储能SOC限制充放电功率
        返回限幅后的功率指令
        """
        soc = self.state.soc

        # 放电SOC限制
        if soc <= self.SOC_CRITICAL:
            # 紧急：只允许充电，禁止放电
            p_bat_ref = min(p_bat_ref, 0.0)
        elif soc <= self.SOC_LOW:
            # 告警：限制放电功率为额定功率的30%
            max_discharge = self.P_BAT_MAX * 0.3
            p_bat_ref = min(p_bat_ref, max_discharge)
        elif soc >= self.SOC_HIGH:
            # 高SOC：只允许放电，禁止充电（或限制充电）
            p_bat_ref = max(p_bat_ref, 0.0)

        # 功率限幅
        p_bat_ref = np.clip(p_bat_ref,
                            self.P_BAT_CHARGE_MAX,
                            self.P_BAT_DISCHARGE_MAX)

        return p_bat_ref

    def load_management(self, delta_p: float) -> None:
        """
        负荷管理：在储能无法完全平衡功率时，管理非重要负荷的投切
        """
        soc = self.state.soc

        # 判断是否需要切除非重要负荷
        need_shedding = False
        need_restore = False

        if soc <= self.SOC_CRITICAL:
            # SOC过低，必须切除非重要负荷
            need_shedding = True
        elif soc <= self.SOC_LOW and delta_p > self.P_BAT_MAX * 0.8:
            # SOC偏低且功率严重不足
            need_shedding = True
        elif soc >= self.SOC_LOW + 10 and delta_p > 0:
            # SOC恢复且功率有缺额，但已经在临界以上，保持当前状态
            pass

        if (soc >= self.SOC_LOW + 15 and
            (self.state.p_pv + self.state.p_wind >
             self.state.p_load_imp + self.state.p_load_nonimp)):
            need_restore = True

        if need_shedding and self.nonimp_load_on:
            self.shed_nonimp_load()
        elif need_restore and not self.nonimp_load_on:
            self.restore_nonimp_load()

    def shed_nonimp_load(self) -> None:
        """切除非重要负荷"""
        self.nonimp_load_on = False
        print(f"[{time.strftime('%H:%M:%S')}] ALARM: 非重要负荷已切除, SOC={self.state.soc:.1f}%")

    def restore_nonimp_load(self) -> None:
        """恢复非重要负荷"""
        if (self.state.p_pv + self.state.p_wind >
            self.state.p_load_imp + self.state.p_load_nonimp + 20):
            # 确认有20kW以上功率裕量后再恢复
            self.nonimp_load_on = True
            print(f"[{time.strftime('%H:%M:%S')}] INFO: 非重要负荷已恢复, SOC={self.state.soc:.1f}%")

    def update_system_frequency(self, p_unbalance: float) -> None:
        """
        更新系统频率（模拟惯量响应）
        实际系统中频率由储能PCS的V/f控制器自动维持
        此处用简化模型模拟
        """
        # 系统惯量时间常数 (s)，模拟频率变化速率
        H = 2.0
        # 额定容量基准 (kVA)，此处取储能额定值+光伏+风电 ≈ 350kVA
        S_base = 350.0

        # 频率变化率 df/dt = (ΔP / S_base) * (f0 / (2H))
        df_dt = (p_unbalance / S_base) * (self.FREQ_RATED / (2 * H))

        # 更新频率（一次频率响应 + 惯量响应）
        self.state.freq += df_dt * self.CONTROL_PERIOD

        # 频率限幅
        self.state.freq = np.clip(self.state.freq,
                                  self.FREQ_MIN,
                                  self.FREQ_MAX)

    def control_cycle(self) -> dict:
        """
        单次控制循环
        返回控制结果字典
        """
        # 1. 读取传感器数据
        self.state = self.read_sensors()

        # 2. 计算功率差额
        delta_p = self.calculate_power_balance()

        # 3. P-f下垂控制
        p_bat_ref = self.pf_droop_control(delta_p)

        # 4. 二次调频补偿
        p_secondary = self.secondary_freq_regulation()
        p_bat_ref += p_secondary

        # 5. SOC保护限幅
        p_bat_ref = self.soc_protection(p_bat_ref)

        # 6. 负荷管理
        self.load_management(delta_p)

        # 7. 执行储能功率指令（实际通过Modbus写入PCS寄存器）
        self.state.p_battery = p_bat_ref
        # modbus_write(addr=3, register=0x300, value=p_bat_ref)

        # 8. 更新系统频率（模拟）
        actual_unbalance = delta_p + p_bat_ref  # 实际剩余不平衡功率
        self.update_system_frequency(actual_unbalance)

        # 9. 更新SOC（模拟）
        # SOC变化率 = -P_bat / (E_rated) (放电SOC下降)
        e_rated = 400.0  # 400kWh
        self.state.soc -= p_bat_ref * self.CONTROL_PERIOD / 3600.0 / e_rated * 100.0
        self.state.soc = np.clip(self.state.soc, 0.0, 100.0)

        return {
            'delta_p': delta_p,
            'p_bat_ref': p_bat_ref,
            'freq': self.state.freq,
            'soc': self.state.soc
        }

    def check_freq_quality(self) -> bool:
        """检查频率是否在合格范围内"""
        return (self.FREQ_MIN <= self.state.freq <= self.FREQ_MAX)


def simulate_scenario():
    """模拟典型运行场景"""
    ctrl = OffGridPowerBalanceController()
    ctrl.state.p_pv = 80.0     # 光伏当前出力80kW
    ctrl.state.p_wind = 30.0   # 风电当前出力30kW
    ctrl.state.p_load_imp = 30.0   # 重要负荷30kW
    ctrl.state.p_load_nonimp = 100.0  # 非重要负荷100kW
    ctrl.state.soc = 65.0      # 初始SOC 65%
    ctrl.state.freq = 50.05    # 初始频率50.05Hz

    print("=" * 60)
    print("离网功率平衡控制仿真")
    print(f"初始状态: P_pv={ctrl.state.p_pv}kW, P_wind={ctrl.state.p_wind}kW")
    print(f"          P_load_imp={ctrl.state.p_load_imp}kW, "
          f"P_load_nonimp={ctrl.state.p_load_nonimp}kW")
    print(f"          SOC={ctrl.state.soc}%, Freq={ctrl.state.freq}Hz")
    print("-" * 60)

    sim_time = 30.0  # 仿真30秒
    dt = ctrl.CONTROL_PERIOD
    steps = int(sim_time / dt)

    # 模拟：t=10s时刻云层遮挡光伏，出力从80kW骤降至30kW
    for i in range(steps):
        t = i * dt

        if t >= 10.0:
            ctrl.state.p_pv = 30.0  # 光伏骤降50kW

        result = ctrl.control_cycle()

        if i % 250 == 0:  # 每5秒打印一次
            freq_ok = ctrl.check_freq_quality()
            flag = "OK" if freq_ok else "WARN"
            print(f"t={t:5.1f}s | ΔP={result['delta_p']:+6.1f}kW | "
                  f"P_bat={result['p_bat_ref']:+7.1f}kW | "
                  f"Freq={result['freq']:5.2f}Hz | "
                  f"SOC={result['soc']:5.1f}% | [{flag}]")

    print("-" * 60)
    print(f"仿真结束: SOC={ctrl.state.soc:.1f}%, Freq={ctrl.state.freq:.2f}Hz")
    print("=" * 60)


if __name__ == "__main__":
    simulate_scenario()
```

**程序说明**：

1. **功率差额计算**：实时计算负荷总量与光伏+风电出力之差，确定储能需提供的平衡功率。

2. **P-f下垂控制**：模拟同步发电机的频率-有功下垂特性，当频率偏离额定值时自动调节储能出力以平衡系统。

3. **二次调频**：通过积分控制消除下垂控制后的稳态频率偏差，将频率精确恢复至50Hz。

4. **SOC保护**：分层保护策略——SOC≤20%限制放电，SOC≤10%禁止放电仅允许充电，SOC≥90%限制充电。

5. **负荷管理**：在SOC过低或功率严重不足时自动切除非重要负荷，条件恢复后自动投入。

6. **控制周期20ms**：保证每个工频周期内完成一次控制计算，满足毫秒级功率平衡响应要求。

---

## 八、参考文献

[1] 王成山. 微电网分析与仿真理论[M]. 北京: 科学出版社, 2013.

[2] 杨新法, 苏剑, 吕志鹏, 等. 微电网技术综述[J]. 中国电机工程学报, 2014, 34(1): 57-70.

[3] IEEE Std 1547.4-2011. IEEE Guide for Design, Operation, and Integration of Distributed Resource Island Systems with Electric Power Systems[S].

[4] 鲁宗相, 王彩霞, 闵勇, 等. 微电网研究综述[J]. 电力系统自动化, 2007, 31(19): 100-107.

[5] Olivares D E, Mehrizi-Sani A, Etemadi A H, et al. Trends in Microgrid Control[J]. IEEE Transactions on Smart Grid, 2014, 5(4): 1905-1919.

[6] GB/T 33589-2017. 微电网接入电力系统技术规定[S]. 北京: 中国标准出版社, 2017.

---

> **说明**：以上内容为参考文本，请根据要求手写于报告纸上，电气图和EMS结构图请按描述手绘。封面信息（姓名、学号、班级等）请自行填写，下划线保持右对齐。每页底部标注页码。
