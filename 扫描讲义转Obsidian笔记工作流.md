# 扫描讲义转 Obsidian 笔记工作流

本工作流用于把扫描版或图片型 PDF 讲义转写为 Obsidian Markdown 底稿。核心原则是：**模型识图忠实转写，但不总结**。

也就是说：把 PDF 页面恢复成可读 Markdown，整理标题、列表、表格、公式和重点术语；但不写“一句话摘要”、不做考点提炼、不新增原文没有的标题，也不改变原文顺序。

# 一、文件准备

## 1. 输入目录

待转写 PDF 放入：

```text
pdf/
```

## 2. 输出目录

转写后的 Markdown 放入：

```text
待排版/
```

输出文件名使用：

```text
<原 PDF 文件名>_模型识图.md
```

例如：

```text
衡真电机学讲义-031-035_模型识图.md
```

## 3. PDF 切分原则

- 可以按固定页数切分，例如每份 3-5 页。
- 文件名最好保留页码范围，例如 `衡真电机学讲义-031-035.pdf`。
- 不强制按章节名切分；转写时按 PDF 原文可见结构保留标题、图号、例题编号和习题编号。

# 二、转写原则

## 1. 绝对禁止

- 不自行总结。
- 不扩写成教材风格。
- 不生成“一句话摘要”“复习摘要”“考点提炼”等内容。
- 不擅自加入原文没有的标题。
- 不改变原文表述的含义和顺序。
- 不默认使用 OCR 脚本。
- 不把 OCR 文本作为主转写来源。

## 2. 默认模型识图流程

1. 读取 `pdf/` 文件夹中的新 PDF。
2. 将 PDF 渲染为逐页图片。
3. 使用模型视觉能力逐页识别页面内容。
4. 按页面顺序转写为 Obsidian Markdown。
5. 对多栏页面，按自然阅读顺序重建正文。
6. 将公式、变量、相量、单位统一转换为 LaTeX。
7. 转写完成后，遍历全文并加粗重点术语。
8. 输出到 `待排版/`，文件名使用 `_模型识图.md` 后缀。

> 只有在用户明确要求 OCR、模型视觉不可用，或需要辅助核对时，才允许 OCR 作为旁证。OCR 不作为默认流程。

## 3. 允许的整理

- 原文有标题时，可以转成 Markdown 标题。
- 原文没有标题时，直接写入正文，不为了好看补标题。
- 原文明显是列表时，可以整理成 Markdown 列表。
- 表格结构清楚时，可以整理成 Markdown 表格。
- 可以合并页面换行和断行，但不得改变句子含义。
- 可以少量加粗核心术语、设备名称、定义、变量和关键词。
- 公式能确认时转为 LaTeX；不能确认时保留不确定标记。
- 很糊的页面不保留大段乱码，只保留能确认的文字并标注不确定。

## 4. 不确定标记

阅读视图中可以隐藏的不确定内容，使用 Obsidian 注释：

```markdown
%% 不确定：原图此处模糊，需对照原 PDF。 %%
```

必须让读者看到的推测内容，使用可见标记：

```markdown
[原文模糊：推测出的内容]
```

推断修正示例：

```markdown
%% 推断修正：图中文字较糊，已按上下文将其校为“磁阻”。 %%
```

# 三、电气工程格式规范

## 1. 公式与变量

- 所有变量、参数、向量、相量、复数和单位都使用 LaTeX。
- 短变量和短表达式使用行内公式，例如 `$P_e$`、`$kV$`、`$MVA$`、`$\Omega$`。
- 独立公式使用公式块，公式块前后保留空行：

```markdown
$$
S = \sqrt{3}UI
$$
```

- 相量默认使用点号表示，例如 `$\dot{U}$`、`$\dot{I}$`、`$\dot{E}$`。
- 复数表达式按电气工程习惯写成 LaTeX，例如 `$E = U + I(R + jX)$`。
- 保留原文可见的下标、上标和相别标记，例如 `$U_{ab}$`、`$I_a$`、`$X_d'$`、`$x_d''$`。

## 2. 单位

单位按原文和电气工程惯例保持一致，例如：

```markdown
`$kV$`、`$A$`、`$MVA$`、`$MW$`、`$Mvar$`、`$\Omega$`、`$pu$`
```

## 3. 图形与图题

- 图号、图题、图注按原文顺序保留。
- 相量图、波形图、特性曲线、等效电路图无法忠实转写时，保留图号或图题。
- 只有用户明确要求重新生成图形时，才添加 Python 绘图提示词。

# 四、处理完成后的 PDF 清理

如果用户要求删除已经转写的 PDF，只删除满足以下条件的文件：

1. PDF 位于 `pdf/` 文件夹；
2. `待排版/` 中存在同名的 `<原 PDF 文件名>_模型识图.md`；
3. 删除前先列出匹配到的 PDF；
4. 不删除没有对应 Markdown 的 PDF。

# 五、验收标准

- 内容顺序与原讲义一致。
- 读起来是原文转写，而不是 AI 总结。
- 没有原文不存在的标题。
- 页面开头没有标题时，正文直接续写。
- 公式、变量、相量和单位能在 Obsidian 中正常渲染。
- 重点术语已加粗，但没有改写成考点总结。
- `%% 不确定 %%` 或 `[原文模糊：...]` 没有被强行确定化。
- 输出文件位于 `待排版/`，文件名带 `_模型识图.md`。
